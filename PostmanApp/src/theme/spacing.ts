export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  xxl: 32
} as const;

// Field workers wear gloves and use the app one-handed outdoors — keep
// interactive targets comfortably above the 44pt platform minimum.
export const touchTarget = {
  minHeight: 48,
  minWidth: 48
} as const;

export const radius = {
  sm: 6,
  md: 10,
  lg: 16,
  pill: 999
} as const;
