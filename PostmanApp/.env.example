# Copy to .env and fill in for local development.
# These are read into app.json's `extra` block via app.config.js — see
# docs/mobile-architecture.md for how expo-constants picks them up.

# Base URL of the existing Node/Express backend's REST API (no trailing slash).
# `localhost` only works from a web browser on the same machine as the backend.
# - Android emulator: use http://10.0.2.2:<port>/api/v1 (emulator's alias for host localhost)
# - Physical device (Android or iOS), Expo Go, or dev-client over LAN:
#     use your development machine's LAN IP, e.g. http://192.168.1.50:4000/api/v1
#     (find it with `ipconfig` on Windows / `ifconfig`|`ip addr` on macOS/Linux;
#     the phone and the machine running the backend must be on the same Wi-Fi network)
API_BASE_URL=http://<YOUR-PC-LAN-IP>:4000/api/v1

# MapLibre-compatible style JSON. Use an OpenStreetMap-based style; do not
# point this at a Google Maps style/service.
MAP_STYLE_URL=https://demotiles.maplibre.org/style.json

# Optional: OSRM (or compatible) road-routing engine base URL, if the backend
# does not already return road-network polylines in the route response.
OSRM_BASE_URL=
